# Public API base URL used by Expo runtime.
# Use HTTPS for non-local hosts.
EXPO_PUBLIC_API_BASE_URL=https://exoinvite.site

# Enable verbose HTTP debug logs in development only.
EXPO_PUBLIC_DEBUG_HTTP_LOGGING=false
